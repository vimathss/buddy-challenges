/**
 * Returns the property names from a profile object.
 *
 * @param {Record<string, unknown>} profile - Object containing profile data.
 * @returns {string[]} Array with all own enumerable property names.
 */
function getProfileFields(profile) {
  throw new Error("Not implemented");
}

module.exports = {
  getProfileFields,
};
