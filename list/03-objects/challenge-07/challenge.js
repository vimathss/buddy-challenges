/**
 * Counts how many values of each JavaScript type exist in an object.
 *
 * @param {Record<string, unknown>} data - Object whose values should be counted by type.
 * @returns {Record<string, number>} Object where keys are `typeof` results and values are counts.
 */
function countValueTypes(data) {
  throw new Error("Not implemented");
}

module.exports = {
  countValueTypes,
};
