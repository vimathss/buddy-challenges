/**
 * Formats inventory entries as readable strings.
 *
 * @param {Record<string, number>} inventory - Object where keys are item names and values are quantities.
 * @returns {string[]} Formatted entries in the "item: quantity" format.
 */
function formatInventory(inventory) {
  throw new Error("Not implemented");
}

module.exports = {
  formatInventory,
};
