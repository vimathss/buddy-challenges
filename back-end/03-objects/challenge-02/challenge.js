/**
 * Sums all numeric score values from an object.
 *
 * @param {Record<string, number>} scores - Object where each value is a score.
 * @returns {number} Sum of all score values.
 */
function sumScores(scores) {
  throw new Error("Not implemented");
}

module.exports = {
  sumScores,
};
